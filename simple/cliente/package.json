{
  "name": "cliente",
  "version": "1.0.0",
  "description": "Cliente COAP",
  "main": "index.ts",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/index.js",
    "dev": "concurrently \"npx tsc --watch\" \"nodemon -q dist/index.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AdP - UBA",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.1.7",
    "concurrently": "^8.0.1",
    "nodemon": "^2.0.22",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "coap": "^1.3.0"
  }
}
